# 前端開發指引 (Frontend Guidelines)

## 1. 核心技術棧
* 框架：React 18+ (使用 Vite 建置)
* 樣式：Tailwind CSS + Lucide Icons
* 狀態管理：Zustand (用於跨元件狀態) 或 React Context (僅限全域設定如 Role/Theme)
* 路由：React Router v6
* API 請求：`axios` (需封裝攔截器處理 JWT Token)

## 2. 程式碼撰寫規範
* 強制使用 Functional Components 與 React Hooks (`useState`, `useEffect`, `useCallback`)，絕對禁止寫 Class Components。
* 元件必須依照功能拆分，保持單一職責原則 (SRP)。過大的元件請拆分為 UI Component 與 Container Component。
* 所有的多行輸入框 (Textarea) 必須套用 `autoResize` 邏輯。
* 過長的文字段落必須實作 `Text Clamp` (顯示更多/隱藏內容) 的切換邏輯。

## 3. UI/UX 規範
* 保母端 (Sitter) 視覺主色系為 Amber (`bg-amber-500` 等)。
* 飼主端 (Client) 視覺主色系為 Blue (`bg-blue-500` 等)。
* 必須確保 RWD (響應式網頁設計) 完美適配手機版型 (限制於 `max-w-md mx-auto` 容器內)。

## 4. AI 助理連續性規範 (AI Continuity)
* 所有的開發進度、任務清單與總結必須即時同步至 `.agent/brain/` 文件夾。
* 每次完成重大任務後，必須執行 `.agents/workflows/persist-progress.md` 中的同步流程。
## 5. API 與型別同步規範 (API & Type Synchronization)
* 當後端 Controller 或 DTO 變更時，**必須**在 `frontend/` 目錄執行 `npm run api:generate` 以同步前端型別與 SDK。
* 嚴禁手寫 `src/services/gen/` 目錄下的任何內容，該目錄應始終由 OpenAPI 規格自動生成。
* 自定義請求邏輯（如攔截器、特殊檔案上傳）應留在 `src/services/api.js` 中。